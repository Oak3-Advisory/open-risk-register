name: open-risk-register
region: ams

static_sites:
  - name: web
    environment_slug: html
    github:
      repo: Oak3-Advisory/open-risk-register
      branch: main
      deploy_on_push: true
    build_command: npm ci && npm run build
    output_dir: build
    index_document: index.html
    error_document: 404.html
